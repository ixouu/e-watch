import React from "react";

const Star = ({ color }) => {
    return (
        <i
        className="fa-solid fa-star"
        style={{color : `${color}`}}
        >
        </i>
    );
}

export default Star;
